import { motion } from "framer-motion";
import VideoBackground from "./Carousel/VideoBackground";

const Home = () => {
  return (
    <div>
      <VideoBackground />
    </div>
  );
};

export default Home;
